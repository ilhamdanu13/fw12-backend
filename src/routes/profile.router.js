// const profileRouter = require("express").Router();
// const uploadProfile = require("../controllers/profile.controller");
// const upploadMiddleware = require("../middlewares/upload.middleware");

// profileRouter.patch("/:id", upploadMiddleware, uploadProfile);
